import { describe, it, expect } from "vitest";

describe("Student Dashboard", () => {
  it("T141: GET /student/dashboard returns progress summary", async () => {
    expect(true).toBe(false); // intentionally failing â€” needs real DB
  });
});

