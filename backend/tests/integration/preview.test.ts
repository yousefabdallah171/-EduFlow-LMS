import { describe, it, expect } from "vitest";

describe("Preview lesson flow", () => {
  it("T127: GET /api/v1/lessons/preview returns lesson data", async () => {
    // This test will fail until is_preview migration runs
    expect(true).toBe(false); // placeholder â€” intentionally failing
  });

  it("T128: preview token validates, non-preview guest rejected", async () => {
    expect(true).toBe(false); // placeholder â€” intentionally failing
  });
});

