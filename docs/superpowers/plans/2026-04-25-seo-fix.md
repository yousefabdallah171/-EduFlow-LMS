# SEO Technical Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all technical SEO issues in the EduFlow LMS React SPA so that Google can index public pages, every page has unique meta tags, and social sharing works correctly.

**Architecture:** Install `react-helmet-async` for per-page head management + `vite-plugin-prerender` to generate static HTML snapshots of 11 public routes at build time. Add `robots.txt`, `sitemap.xml`, `og-image.png`, canonical tags, hreflang, and JSON-LD structured data.

**Tech Stack:** React 18, Vite 5, react-helmet-async, vite-plugin-prerender, react-router-dom v6, i18next

---

## File Map

### New Files
| File | Responsibility |
|------|---------------|
| `frontend/src/components/shared/SEO.tsx` | Reusable Helmet component — per-page title, description, canonical, og, twitter, hreflang, JSON-LD |
| `frontend/src/lib/seo-config.ts` | SEO data for every route in EN + AR |
| `frontend/public/robots.txt` | Crawl rules — allow public, block /admin /dashboard /api |
| `frontend/public/sitemap.xml` | All 11 public URLs with hreflang alternates |
| `frontend/public/og-image.png` | 1200×630 social share image placeholder |

### Modified Files
| File | Change |
|------|--------|
| `frontend/package.json` | Add `react-helmet-async`, `vite-plugin-prerender` |
| `frontend/src/main.tsx` | Wrap with `<HelmetProvider>` |
| `frontend/index.html` | Add og:image, og:url, fix twitter:card → summary_large_image, add fallback canonical |
| `frontend/vite.config.ts` | Add prerender plugin with 11 public routes |
| `frontend/src/pages/Landing.tsx` | Add `<SEO>` with Course structured data |
| `frontend/src/pages/About.tsx` | Add `<SEO>` |
| `frontend/src/pages/FAQ.tsx` | Add `<SEO>` with FAQPage structured data |
| `frontend/src/pages/Pricing.tsx` | Add `<SEO>` (public Pricing page) |
| `frontend/src/pages/Contact.tsx` | Add `<SEO>` |
| `frontend/src/pages/Testimonials.tsx` | Add `<SEO>` |
| `frontend/src/pages/Roadmap.tsx` | Add `<SEO>` |
| `frontend/src/pages/PrivacyPolicy.tsx` | Add `<SEO>` |
| `frontend/src/pages/Terms.tsx` | Add `<SEO>` |
| `frontend/src/pages/RefundPolicy.tsx` | Add `<SEO>` |
| `frontend/src/pages/Course.tsx` | Add `<SEO>` |
| `frontend/src/pages/Login.tsx` | Add `<SEO noIndex>` |
| `frontend/src/pages/Register.tsx` | Add `<SEO noIndex>` |
| `frontend/src/pages/ForgotPassword.tsx` | Add `<SEO noIndex>` |
| `frontend/src/pages/ResetPassword.tsx` | Add `<SEO noIndex>` |
| `frontend/src/pages/VerifyEmail.tsx` | Add `<SEO noIndex>` |
| `frontend/src/pages/Checkout.tsx` | Add `<SEO noIndex>` |
| `frontend/src/pages/PaymentSuccess.tsx` | Add `<SEO noIndex>` |
| `frontend/src/pages/PaymentFailure.tsx` | Add `<SEO noIndex>` |
| `frontend/src/pages/PaymentPending.tsx` | Add `<SEO noIndex>` |
| `frontend/src/pages/PaymentHistory.tsx` | Add `<SEO noIndex>` |
| `frontend/src/pages/NotFound.tsx` | Add `<SEO noIndex>` with 404 title |
| `docs/features/LANDING_PAGE.md` | Add SEO section |

---

## Task 1: Install Dependencies

**Files:**
- Modify: `frontend/package.json`

- [ ] **Step 1.1: Install react-helmet-async and vite-plugin-prerender**

```bash
cd frontend
npm install react-helmet-async
npm install --save-dev vite-plugin-prerender
```

- [ ] **Step 1.2: Verify packages installed**

```bash
cat package.json | grep -E "react-helmet|prerender"
```

Expected output:
```
"react-helmet-async": "^2.0.5",
"vite-plugin-prerender": "^1.0.5"
```

- [ ] **Step 1.3: Commit**

```bash
cd ..
git add frontend/package.json frontend/package-lock.json
git commit -m "feat(seo): install react-helmet-async and vite-plugin-prerender"
```

---

## Task 2: Create SEO Config (`seo-config.ts`)

**Files:**
- Create: `frontend/src/lib/seo-config.ts`

- [ ] **Step 2.1: Create the SEO config file**

Create `frontend/src/lib/seo-config.ts`:

```typescript
export const SITE_URL = "https://yousef-abdallah.online";
export const SITE_NAME = "Yousef Abdallah Course";
export const DEFAULT_OG_IMAGE = `${SITE_URL}/og-image.png`;

export interface PageSEO {
  titleEn: string;
  titleAr: string;
  descriptionEn: string;
  descriptionAr: string;
  canonical: string;
  ogImage?: string;
  ogType?: "website" | "article";
  noIndex?: boolean;
}

export const SEO_PAGES: Record<string, PageSEO> = {
  landing: {
    titleEn: "Build a Full Production App with AI — Yousef Abdallah",
    titleAr: "ابنِ تطبيقاً كاملاً بالذكاء الاصطناعي — يوسف عبدالله",
    descriptionEn:
      "A practical hands-on course: from idea to PRD, design, implementation, security, testing, and deployment. Build a real production app step by step.",
    descriptionAr:
      "دورة عملية شاملة: من الفكرة إلى المنتج الحقيقي — تصميم، تطوير، أمان، اختبار، ونشر. ابنِ تطبيقاً حقيقياً خطوة بخطوة.",
    canonical: "/",
    ogImage: DEFAULT_OG_IMAGE,
    ogType: "website",
  },
  course: {
    titleEn: "Course Content — 7 Phases | Yousef Abdallah",
    titleAr: "محتوى الدورة — 7 مراحل | يوسف عبدالله",
    descriptionEn:
      "Explore all 7 course phases: Planning, Design, Implementation, Review, Security & Performance, Testing, and Deployment.",
    descriptionAr:
      "استكشف 7 مراحل الدورة: التخطيط، التصميم، التطوير، المراجعة، الأمان والأداء، الاختبار، والنشر.",
    canonical: "/course",
    ogImage: DEFAULT_OG_IMAGE,
  },
  pricing: {
    titleEn: "Pricing — Enroll Today | Yousef Abdallah",
    titleAr: "الأسعار — سجّل الآن | يوسف عبدالله",
    descriptionEn:
      "One-time payment. Lifetime access. Get full access to all course content and build a production-grade app.",
    descriptionAr:
      "دفعة واحدة. وصول مدى الحياة. احصل على وصول كامل لجميع محتوى الدورة.",
    canonical: "/pricing",
    ogImage: DEFAULT_OG_IMAGE,
  },
  about: {
    titleEn: "About Yousef Abdallah — Full-Stack Developer & Instructor",
    titleAr: "عن يوسف عبدالله — مطور ومدرب",
    descriptionEn:
      "Yousef Abdallah is a full-stack developer and instructor teaching production-grade app development using AI-powered workflows.",
    descriptionAr:
      "يوسف عبدالله مطور ومدرب متخصص في تطوير التطبيقات الاحترافية باستخدام أدوات الذكاء الاصطناعي.",
    canonical: "/about",
    ogImage: DEFAULT_OG_IMAGE,
  },
  faq: {
    titleEn: "FAQ — Frequently Asked Questions | Yousef Abdallah",
    titleAr: "الأسئلة الشائعة | يوسف عبدالله",
    descriptionEn:
      "Answers to common questions about prerequisites, course content, refund policy, and how to get started.",
    descriptionAr:
      "إجابات على الأسئلة الشائعة حول المتطلبات والمحتوى وسياسة الاسترداد وكيفية البدء.",
    canonical: "/faq",
  },
  contact: {
    titleEn: "Contact Us | Yousef Abdallah",
    titleAr: "تواصل معنا | يوسف عبدالله",
    descriptionEn: "Get in touch with Yousef Abdallah for course questions, support, or business inquiries.",
    descriptionAr: "تواصل مع يوسف عبدالله للاستفسار عن الدورة أو للحصول على الدعم.",
    canonical: "/contact",
  },
  testimonials: {
    titleEn: "Student Testimonials | Yousef Abdallah",
    titleAr: "آراء الطلاب | يوسف عبدالله",
    descriptionEn: "See what students say about the course and their experience building a production app.",
    descriptionAr: "اقرأ تجارب الطلاب وآراءهم حول بناء تطبيق احترافي.",
    canonical: "/testimonials",
    ogImage: DEFAULT_OG_IMAGE,
  },
  roadmap: {
    titleEn: "Course Roadmap | Yousef Abdallah",
    titleAr: "خارطة الدورة | يوسف عبدالله",
    descriptionEn: "See the full learning path: from zero to a deployed, production-ready application.",
    descriptionAr: "اطلع على مسار التعلم الكامل: من الصفر إلى تطبيق جاهز للإنتاج.",
    canonical: "/roadmap",
  },
  privacy: {
    titleEn: "Privacy Policy | Yousef Abdallah",
    titleAr: "سياسة الخصوصية | يوسف عبدالله",
    descriptionEn: "Read our privacy policy to understand how we collect, use, and protect your data.",
    descriptionAr: "اقرأ سياسة الخصوصية لفهم كيفية جمع بياناتك واستخدامها وحمايتها.",
    canonical: "/privacy",
    noIndex: false,
  },
  terms: {
    titleEn: "Terms of Service | Yousef Abdallah",
    titleAr: "شروط الخدمة | يوسف عبدالله",
    descriptionEn: "Read the terms of service for using the Yousef Abdallah course platform.",
    descriptionAr: "اقرأ شروط الخدمة لاستخدام منصة دورة يوسف عبدالله.",
    canonical: "/terms",
  },
  refund: {
    titleEn: "Refund Policy | Yousef Abdallah",
    titleAr: "سياسة الاسترداد | يوسف عبدالله",
    descriptionEn: "Learn about our refund policy and how to request a refund if you're not satisfied.",
    descriptionAr: "تعرف على سياسة استرداد الأموال وكيفية تقديم طلب الاسترداد.",
    canonical: "/refund",
  },
  // noIndex pages
  login: {
    titleEn: "Login | Yousef Abdallah",
    titleAr: "تسجيل الدخول | يوسف عبدالله",
    descriptionEn: "Sign in to your Yousef Abdallah course account.",
    descriptionAr: "سجّل دخولك إلى حساب دورة يوسف عبدالله.",
    canonical: "/login",
    noIndex: true,
  },
  register: {
    titleEn: "Create Account | Yousef Abdallah",
    titleAr: "إنشاء حساب | يوسف عبدالله",
    descriptionEn: "Create your account to get started with the course.",
    descriptionAr: "أنشئ حسابك للبدء في الدورة.",
    canonical: "/register",
    noIndex: true,
  },
  forgotPassword: {
    titleEn: "Reset Password | Yousef Abdallah",
    titleAr: "إعادة تعيين كلمة المرور | يوسف عبدالله",
    descriptionEn: "Reset your account password.",
    descriptionAr: "إعادة تعيين كلمة مرور حسابك.",
    canonical: "/forgot-password",
    noIndex: true,
  },
  checkout: {
    titleEn: "Checkout | Yousef Abdallah",
    titleAr: "إتمام الشراء | يوسف عبدالله",
    descriptionEn: "Complete your enrollment purchase.",
    descriptionAr: "أتمم عملية الشراء والتسجيل.",
    canonical: "/checkout",
    noIndex: true,
  },
  paymentSuccess: {
    titleEn: "Payment Successful | Yousef Abdallah",
    titleAr: "تمت عملية الدفع بنجاح | يوسف عبدالله",
    descriptionEn: "Your payment was successful. Welcome to the course!",
    descriptionAr: "تمت عملية الدفع بنجاح. أهلاً بك في الدورة!",
    canonical: "/payment/success",
    noIndex: true,
  },
  paymentFailure: {
    titleEn: "Payment Failed | Yousef Abdallah",
    titleAr: "فشلت عملية الدفع | يوسف عبدالله",
    descriptionEn: "Your payment could not be processed. Please try again.",
    descriptionAr: "تعذّر إتمام عملية الدفع. يرجى المحاولة مجدداً.",
    canonical: "/payment/failure",
    noIndex: true,
  },
  notFound: {
    titleEn: "404 — Page Not Found | Yousef Abdallah",
    titleAr: "404 — الصفحة غير موجودة | يوسف عبدالله",
    descriptionEn: "This page does not exist.",
    descriptionAr: "هذه الصفحة غير موجودة.",
    canonical: "/404",
    noIndex: true,
  },
};
```

- [ ] **Step 2.2: Commit**

```bash
git add frontend/src/lib/seo-config.ts
git commit -m "feat(seo): add per-page SEO config for all routes"
```

---

## Task 3: Create `SEO.tsx` Component

**Files:**
- Create: `frontend/src/components/shared/SEO.tsx`

- [ ] **Step 3.1: Create SEO component**

Create `frontend/src/components/shared/SEO.tsx`:

```tsx
import { Helmet } from "react-helmet-async";
import { useTranslation } from "react-i18next";
import { SITE_NAME, SITE_URL, DEFAULT_OG_IMAGE, type PageSEO } from "@/lib/seo-config";

interface SEOProps {
  page: PageSEO;
  structuredData?: object | object[];
}

export const SEO = ({ page, structuredData }: SEOProps) => {
  const { i18n } = useTranslation();
  const isArabic = i18n.language === "ar";

  const title = isArabic ? page.titleAr : page.titleEn;
  const description = isArabic ? page.descriptionAr : page.descriptionEn;
  const canonicalUrl = `${SITE_URL}${page.canonical}`;
  const ogImage = page.ogImage ?? DEFAULT_OG_IMAGE;
  const ogType = page.ogType ?? "website";

  const jsonLdScripts = structuredData
    ? Array.isArray(structuredData)
      ? structuredData
      : [structuredData]
    : [];

  return (
    <Helmet>
      {/* Primary */}
      <html lang={isArabic ? "ar" : "en"} dir={isArabic ? "rtl" : "ltr"} />
      <title>{title}</title>
      <meta name="description" content={description} />
      <link rel="canonical" href={canonicalUrl} />

      {/* Robots */}
      {page.noIndex ? (
        <meta name="robots" content="noindex,nofollow" />
      ) : (
        <meta name="robots" content="index,follow" />
      )}

      {/* Open Graph */}
      <meta property="og:type" content={ogType} />
      <meta property="og:site_name" content={SITE_NAME} />
      <meta property="og:title" content={title} />
      <meta property="og:description" content={description} />
      <meta property="og:url" content={canonicalUrl} />
      <meta property="og:image" content={ogImage} />
      <meta property="og:image:width" content="1200" />
      <meta property="og:image:height" content="630" />
      <meta property="og:locale" content={isArabic ? "ar_EG" : "en_US"} />
      <meta
        property="og:locale:alternate"
        content={isArabic ? "en_US" : "ar_EG"}
      />

      {/* Twitter Card */}
      <meta name="twitter:card" content="summary_large_image" />
      <meta name="twitter:title" content={title} />
      <meta name="twitter:description" content={description} />
      <meta name="twitter:image" content={ogImage} />

      {/* hreflang */}
      {!page.noIndex && (
        <>
          <link
            rel="alternate"
            hrefLang="en"
            href={`${SITE_URL}${page.canonical}`}
          />
          <link
            rel="alternate"
            hrefLang="ar"
            href={`${SITE_URL}/ar${page.canonical}`}
          />
          <link
            rel="alternate"
            hrefLang="x-default"
            href={`${SITE_URL}${page.canonical}`}
          />
        </>
      )}

      {/* JSON-LD Structured Data */}
      {jsonLdScripts.map((schema, i) => (
        <script
          key={i}
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }}
        />
      ))}
    </Helmet>
  );
};
```

- [ ] **Step 3.2: Commit**

```bash
git add frontend/src/components/shared/SEO.tsx
git commit -m "feat(seo): add SEO helmet component with og, twitter, hreflang, JSON-LD"
```

---

## Task 4: Wrap App with HelmetProvider

**Files:**
- Modify: `frontend/src/main.tsx`

- [ ] **Step 4.1: Add HelmetProvider to main.tsx**

Edit `frontend/src/main.tsx` — add import and wrap:

```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import { HelmetProvider } from "react-helmet-async";
import { QueryClientProvider } from "@tanstack/react-query";
import { I18nextProvider } from "react-i18next";
import { Toaster } from "sonner";

import App from "./App";
import { queryClient } from "@/lib/api";
import i18n from "@/lib/i18n";
import { initFrontendSentry } from "./observability/sentry";
import { registerServiceWorker } from "./service-worker";
import "@/styles/globals.css";

initFrontendSentry();

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <HelmetProvider>
      <I18nextProvider i18n={i18n}>
        <QueryClientProvider client={queryClient}>
          <App />
          <Toaster richColors />
        </QueryClientProvider>
      </I18nextProvider>
    </HelmetProvider>
  </React.StrictMode>
);

registerServiceWorker();
```

- [ ] **Step 4.2: Verify app still compiles**

```bash
cd frontend && npm run build 2>&1 | tail -5
```

Expected: `✓ built in` with no errors.

- [ ] **Step 4.3: Commit**

```bash
git add frontend/src/main.tsx
git commit -m "feat(seo): wrap app with HelmetProvider"
```

---

## Task 5: Fix `index.html` Static Meta Tags

**Files:**
- Modify: `frontend/index.html`

- [ ] **Step 5.1: Update index.html head section**

Replace the `<title>` through the last `<meta name="twitter:...">` tag with:

```html
    <title>Build a Full Production App with AI — Yousef Abdallah</title>
    <meta name="description" content="A practical hands-on course: from idea to PRD, design, implementation, security, testing, and deployment. By Yousef Abdallah." />

    <!-- Canonical fallback (overridden per-page by react-helmet-async) -->
    <link rel="canonical" href="https://yousef-abdallah.online/" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Yousef Abdallah Course" />
    <meta property="og:title" content="Build a Full Production App with AI — Yousef Abdallah" />
    <meta property="og:description" content="A practical hands-on course: from idea to PRD, design, implementation, security, testing, and deployment." />
    <meta property="og:url" content="https://yousef-abdallah.online/" />
    <meta property="og:image" content="https://yousef-abdallah.online/og-image.png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content="en_US" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Build a Full Production App with AI — Yousef Abdallah" />
    <meta name="twitter:description" content="A practical hands-on course: from idea to PRD, design, implementation, security, testing, and deployment." />
    <meta name="twitter:image" content="https://yousef-abdallah.online/og-image.png" />

    <!-- Robots -->
    <meta name="robots" content="index,follow" />
```

- [ ] **Step 5.2: Commit**

```bash
git add frontend/index.html
git commit -m "fix(seo): update index.html with og:image, canonical, correct twitter:card"
```

---

## Task 6: Create `robots.txt` and `sitemap.xml`

**Files:**
- Create: `frontend/public/robots.txt`
- Create: `frontend/public/sitemap.xml`

- [ ] **Step 6.1: Create robots.txt**

Create `frontend/public/robots.txt`:

```
User-agent: *
Allow: /

# Block authenticated and private pages
Disallow: /dashboard
Disallow: /lessons
Disallow: /progress
Disallow: /notes
Disallow: /downloads
Disallow: /orders
Disallow: /profile
Disallow: /help
Disallow: /admin
Disallow: /login
Disallow: /register
Disallow: /checkout
Disallow: /payment
Disallow: /forgot-password
Disallow: /reset-password
Disallow: /verify-email
Disallow: /auth/
Disallow: /api/

# Block locale-prefixed versions of private pages
Disallow: /en/dashboard
Disallow: /en/lessons
Disallow: /en/admin
Disallow: /ar/dashboard
Disallow: /ar/lessons
Disallow: /ar/admin

Sitemap: https://yousef-abdallah.online/sitemap.xml
```

- [ ] **Step 6.2: Create sitemap.xml**

Create `frontend/public/sitemap.xml`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset
  xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">

  <url>
    <loc>https://yousef-abdallah.online/</loc>
    <lastmod>2026-04-25</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
    <xhtml:link rel="alternate" hreflang="en" href="https://yousef-abdallah.online/"/>
    <xhtml:link rel="alternate" hreflang="ar" href="https://yousef-abdallah.online/ar/"/>
  </url>

  <url>
    <loc>https://yousef-abdallah.online/course</loc>
    <lastmod>2026-04-25</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.9</priority>
    <xhtml:link rel="alternate" hreflang="en" href="https://yousef-abdallah.online/course"/>
    <xhtml:link rel="alternate" hreflang="ar" href="https://yousef-abdallah.online/ar/course"/>
  </url>

  <url>
    <loc>https://yousef-abdallah.online/pricing</loc>
    <lastmod>2026-04-25</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.9</priority>
    <xhtml:link rel="alternate" hreflang="en" href="https://yousef-abdallah.online/pricing"/>
    <xhtml:link rel="alternate" hreflang="ar" href="https://yousef-abdallah.online/ar/pricing"/>
  </url>

  <url>
    <loc>https://yousef-abdallah.online/about</loc>
    <lastmod>2026-04-25</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.7</priority>
    <xhtml:link rel="alternate" hreflang="en" href="https://yousef-abdallah.online/about"/>
    <xhtml:link rel="alternate" hreflang="ar" href="https://yousef-abdallah.online/ar/about"/>
  </url>

  <url>
    <loc>https://yousef-abdallah.online/faq</loc>
    <lastmod>2026-04-25</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
    <xhtml:link rel="alternate" hreflang="en" href="https://yousef-abdallah.online/faq"/>
    <xhtml:link rel="alternate" hreflang="ar" href="https://yousef-abdallah.online/ar/faq"/>
  </url>

  <url>
    <loc>https://yousef-abdallah.online/contact</loc>
    <lastmod>2026-04-25</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.6</priority>
    <xhtml:link rel="alternate" hreflang="en" href="https://yousef-abdallah.online/contact"/>
    <xhtml:link rel="alternate" hreflang="ar" href="https://yousef-abdallah.online/ar/contact"/>
  </url>

  <url>
    <loc>https://yousef-abdallah.online/testimonials</loc>
    <lastmod>2026-04-25</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.6</priority>
    <xhtml:link rel="alternate" hreflang="en" href="https://yousef-abdallah.online/testimonials"/>
    <xhtml:link rel="alternate" hreflang="ar" href="https://yousef-abdallah.online/ar/testimonials"/>
  </url>

  <url>
    <loc>https://yousef-abdallah.online/roadmap</loc>
    <lastmod>2026-04-25</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.6</priority>
    <xhtml:link rel="alternate" hreflang="en" href="https://yousef-abdallah.online/roadmap"/>
    <xhtml:link rel="alternate" hreflang="ar" href="https://yousef-abdallah.online/ar/roadmap"/>
  </url>

  <url>
    <loc>https://yousef-abdallah.online/privacy</loc>
    <lastmod>2026-04-25</lastmod>
    <changefreq>yearly</changefreq>
    <priority>0.3</priority>
    <xhtml:link rel="alternate" hreflang="en" href="https://yousef-abdallah.online/privacy"/>
    <xhtml:link rel="alternate" hreflang="ar" href="https://yousef-abdallah.online/ar/privacy"/>
  </url>

  <url>
    <loc>https://yousef-abdallah.online/terms</loc>
    <lastmod>2026-04-25</lastmod>
    <changefreq>yearly</changefreq>
    <priority>0.3</priority>
    <xhtml:link rel="alternate" hreflang="en" href="https://yousef-abdallah.online/terms"/>
    <xhtml:link rel="alternate" hreflang="ar" href="https://yousef-abdallah.online/ar/terms"/>
  </url>

  <url>
    <loc>https://yousef-abdallah.online/refund</loc>
    <lastmod>2026-04-25</lastmod>
    <changefreq>yearly</changefreq>
    <priority>0.3</priority>
    <xhtml:link rel="alternate" hreflang="en" href="https://yousef-abdallah.online/refund"/>
    <xhtml:link rel="alternate" hreflang="ar" href="https://yousef-abdallah.online/ar/refund"/>
  </url>

</urlset>
```

- [ ] **Step 6.3: Commit**

```bash
git add frontend/public/robots.txt frontend/public/sitemap.xml
git commit -m "feat(seo): add robots.txt and sitemap.xml with hreflang alternates"
```

---

## Task 7: Add `<SEO>` to All Public Pages

**Files:** All 11 public page files + 8 noIndex pages

For each page below, the pattern is identical — import SEO and SEO_PAGES, add `<SEO>` as the first child.

- [ ] **Step 7.1: Add SEO to Landing.tsx**

In `frontend/src/pages/Landing.tsx`, add after existing imports:
```tsx
import { SEO } from "@/components/shared/SEO";
import { SEO_PAGES } from "@/lib/seo-config";
```

Inside the `Landing` component's return, add as **first child inside the outermost element**:
```tsx
<SEO page={SEO_PAGES.landing} />
```

- [ ] **Step 7.2: Add SEO to About.tsx**

```tsx
import { SEO } from "@/components/shared/SEO";
import { SEO_PAGES } from "@/lib/seo-config";
// Inside return: <SEO page={SEO_PAGES.about} />
```

- [ ] **Step 7.3: Add SEO to FAQ.tsx**

```tsx
import { SEO } from "@/components/shared/SEO";
import { SEO_PAGES } from "@/lib/seo-config";
// Inside return: <SEO page={SEO_PAGES.faq} />
```

- [ ] **Step 7.4: Add SEO to Pricing.tsx (public)**

```tsx
import { SEO } from "@/components/shared/SEO";
import { SEO_PAGES } from "@/lib/seo-config";
// Inside return: <SEO page={SEO_PAGES.pricing} />
```

- [ ] **Step 7.5: Add SEO to Contact.tsx**

```tsx
import { SEO } from "@/components/shared/SEO";
import { SEO_PAGES } from "@/lib/seo-config";
// Inside return: <SEO page={SEO_PAGES.contact} />
```

- [ ] **Step 7.6: Add SEO to Testimonials.tsx**

```tsx
import { SEO } from "@/components/shared/SEO";
import { SEO_PAGES } from "@/lib/seo-config";
// Inside return: <SEO page={SEO_PAGES.testimonials} />
```

- [ ] **Step 7.7: Add SEO to Roadmap.tsx**

```tsx
import { SEO } from "@/components/shared/SEO";
import { SEO_PAGES } from "@/lib/seo-config";
// Inside return: <SEO page={SEO_PAGES.roadmap} />
```

- [ ] **Step 7.8: Add SEO to PrivacyPolicy.tsx**

```tsx
import { SEO } from "@/components/shared/SEO";
import { SEO_PAGES } from "@/lib/seo-config";
// Inside return: <SEO page={SEO_PAGES.privacy} />
```

- [ ] **Step 7.9: Add SEO to Terms.tsx**

```tsx
import { SEO } from "@/components/shared/SEO";
import { SEO_PAGES } from "@/lib/seo-config";
// Inside return: <SEO page={SEO_PAGES.terms} />
```

- [ ] **Step 7.10: Add SEO to RefundPolicy.tsx**

```tsx
import { SEO } from "@/components/shared/SEO";
import { SEO_PAGES } from "@/lib/seo-config";
// Inside return: <SEO page={SEO_PAGES.refund} />
```

- [ ] **Step 7.11: Add SEO to Course.tsx**

```tsx
import { SEO } from "@/components/shared/SEO";
import { SEO_PAGES } from "@/lib/seo-config";
// Inside return: <SEO page={SEO_PAGES.course} />
```

- [ ] **Step 7.12: Add noIndex SEO to auth/checkout pages**

Add `<SEO page={SEO_PAGES.login} />` to `Login.tsx`  
Add `<SEO page={SEO_PAGES.register} />` to `Register.tsx`  
Add `<SEO page={SEO_PAGES.forgotPassword} />` to `ForgotPassword.tsx`  
Add `<SEO page={SEO_PAGES.checkout} />` to `Checkout.tsx`  
Add `<SEO page={SEO_PAGES.paymentSuccess} />` to `PaymentSuccess.tsx`  
Add `<SEO page={SEO_PAGES.paymentFailure} />` to `PaymentFailure.tsx`  
Add `<SEO page={SEO_PAGES.notFound} />` to `NotFound.tsx`

For each, pattern is:
```tsx
import { SEO } from "@/components/shared/SEO";
import { SEO_PAGES } from "@/lib/seo-config";
// Add as first child in return: <SEO page={SEO_PAGES.login} />
```

- [ ] **Step 7.13: Verify build compiles**

```bash
cd frontend && npm run build 2>&1 | tail -10
```

Expected: `✓ built in` with no TypeScript errors.

- [ ] **Step 7.14: Commit**

```bash
git add frontend/src/pages/
git commit -m "feat(seo): add per-page SEO component to all public and noindex pages"
```

---

## Task 8: Add JSON-LD Structured Data

**Files:**
- Modify: `frontend/src/pages/Landing.tsx`
- Modify: `frontend/src/pages/FAQ.tsx`
- Modify: `frontend/src/pages/About.tsx`

- [ ] **Step 8.1: Add Course + Organization schema to Landing.tsx**

In `Landing.tsx`, update the `<SEO>` call:

```tsx
const courseSchema = {
  "@context": "https://schema.org",
  "@type": "Course",
  "name": "Build a Full Production App with AI",
  "description": "Practical hands-on course: from idea to PRD, design, implementation, security, testing, and deployment.",
  "provider": {
    "@type": "Person",
    "name": "Yousef Abdallah",
    "url": "https://yousef-abdallah.online/about"
  },
  "offers": {
    "@type": "Offer",
    "priceCurrency": "EGP",
    "availability": "https://schema.org/InStock",
    "url": "https://yousef-abdallah.online/pricing"
  },
  "inLanguage": ["en", "ar"],
  "educationalLevel": "Intermediate",
  "url": "https://yousef-abdallah.online/"
};

const orgSchema = {
  "@context": "https://schema.org",
  "@type": "Person",
  "name": "Yousef Abdallah",
  "url": "https://yousef-abdallah.online",
  "knowsAbout": ["Full-Stack Development", "AI-powered development", "React", "Node.js", "TypeScript"]
};

// In the return:
<SEO page={SEO_PAGES.landing} structuredData={[courseSchema, orgSchema]} />
```

- [ ] **Step 8.2: Add FAQPage schema to FAQ.tsx**

In `FAQ.tsx`, read the FAQ items and add structured data. Add above the return:

```tsx
const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "What are the prerequisites for this course?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Basic programming knowledge is helpful but not required. The course is designed to be accessible to motivated beginners."
      }
    },
    {
      "@type": "Question",
      "name": "Do I get lifetime access?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Yes. One payment gives you lifetime access to all course content including future updates."
      }
    },
    {
      "@type": "Question",
      "name": "Is there a refund policy?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Yes. We offer refunds as described in our refund policy at yousef-abdallah.online/refund."
      }
    },
    {
      "@type": "Question",
      "name": "Is the course available in Arabic?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Yes. The course platform fully supports Arabic and all content is bilingual (English and Arabic)."
      }
    }
  ]
};

// In return:
<SEO page={SEO_PAGES.faq} structuredData={faqSchema} />
```

- [ ] **Step 8.3: Add Person schema to About.tsx**

```tsx
const personSchema = {
  "@context": "https://schema.org",
  "@type": "Person",
  "name": "Yousef Abdallah",
  "jobTitle": "Full-Stack Developer & Instructor",
  "url": "https://yousef-abdallah.online/about",
  "knowsAbout": ["Full-Stack Development", "React", "Node.js", "TypeScript", "AI tools"]
};

// In return:
<SEO page={SEO_PAGES.about} structuredData={personSchema} />
```

- [ ] **Step 8.4: Commit**

```bash
git add frontend/src/pages/Landing.tsx frontend/src/pages/FAQ.tsx frontend/src/pages/About.tsx
git commit -m "feat(seo): add JSON-LD structured data for Course, FAQ, and Person schemas"
```

---

## Task 9: Configure `vite-plugin-prerender`

**Files:**
- Modify: `frontend/vite.config.ts`

- [ ] **Step 9.1: Update vite.config.ts with prerender plugin**

Replace the plugins array and add prerender config:

```typescript
/// <reference types="vitest" />

import { defineConfig, loadEnv } from "vite";
import react from "@vitejs/plugin-react";
import prerender from "vite-plugin-prerender";
import path from "node:path";
import { fileURLToPath } from "node:url";

export default defineConfig(({ mode }) => {
  const env = loadEnv(mode, process.cwd(), "");
  const apiProxyTarget = env.VITE_API_PROXY_TARGET || "http://127.0.0.1:3000";
  const currentDir = path.dirname(fileURLToPath(import.meta.url));
  const isProduction = mode === "production";

  return {
    plugins: [
      react(),
      ...(isProduction
        ? [
            prerender({
              staticDir: path.join(currentDir, "dist"),
              routes: [
                "/",
                "/course",
                "/pricing",
                "/about",
                "/faq",
                "/contact",
                "/testimonials",
                "/roadmap",
                "/privacy",
                "/terms",
                "/refund",
              ],
              renderer: new prerender.PuppeteerRenderer({
                renderAfterDocumentEvent: "render-event",
                timeout: 10000,
              }),
            }),
          ]
        : []),
    ],
    server: {
      watch: {
        usePolling: true,
        interval: 300,
      },
      proxy: {
        "/api": {
          target: apiProxyTarget,
          changeOrigin: true,
        },
      },
    },
    resolve: {
      alias: {
        "@": path.resolve(currentDir, "./src"),
      },
    },
    build: {
      chunkSizeWarningLimit: 600,
      rollupOptions: {
        output: {
          manualChunks: {
            react: ["react", "react-dom", "react-router-dom"],
            query: ["@tanstack/react-query", "axios", "zustand"],
            i18n: ["i18next", "react-i18next", "i18next-browser-languagedetector"],
            ui: ["@headlessui/react", "@floating-ui/react", "lucide-react"],
            hls: ["hls.js"],
          },
        },
      },
    },
    test: {
      exclude: ["node_modules", "dist", "tests/e2e/**"],
      passWithNoTests: true,
    },
  };
});
```

- [ ] **Step 9.2: Dispatch the render-event in App.tsx**

The prerender plugin waits for `render-event` before snapshotting. Add this to `frontend/src/App.tsx` after the app mounts:

Find `App.tsx` and add a one-time event dispatch after the component tree renders. Add this inside the `App` component function body:

```tsx
import { useEffect } from "react";

// Inside App component:
useEffect(() => {
  // Signal to vite-plugin-prerender that the app has rendered
  document.dispatchEvent(new Event("render-event"));
}, []);
```

- [ ] **Step 9.3: Run production build and verify pre-rendered HTML**

```bash
cd frontend && npm run build
```

After build, check that pre-rendered HTML files exist:

```bash
ls dist/about/ dist/faq/ dist/pricing/ 2>/dev/null || echo "Check dist/ folder structure"
cat dist/index.html | grep -c "<title>" && echo "title tags found"
cat dist/index.html | grep "Build a Full"
```

Expected: `dist/about/index.html` exists with real HTML content (not empty div).

- [ ] **Step 9.4: Commit**

```bash
git add frontend/vite.config.ts frontend/src/App.tsx
git commit -m "feat(seo): configure vite-plugin-prerender for 11 public routes"
```

---

## Task 10: Create OG Image Placeholder

**Files:**
- Create: `frontend/public/og-image.png`

- [ ] **Step 10.1: Create a placeholder og-image**

The og-image must be 1200×630px. Create a minimal SVG-based placeholder until a proper design is made:

```bash
cat > /tmp/og-image.svg << 'EOF'
<svg width="1200" height="630" viewBox="0 0 1200 630" xmlns="http://www.w3.org/2000/svg">
  <rect width="1200" height="630" fill="#0f0f10"/>
  <rect x="60" y="60" width="1080" height="510" rx="16" fill="#18181b" stroke="#27272a" stroke-width="2"/>
  <text x="600" y="260" font-family="system-ui,sans-serif" font-size="64" font-weight="700" fill="#ffffff" text-anchor="middle">Build a Production App</text>
  <text x="600" y="340" font-family="system-ui,sans-serif" font-size="36" fill="#a1a1aa" text-anchor="middle">with AI — Yousef Abdallah</text>
  <rect x="490" y="400" width="220" height="56" rx="8" fill="#f59e0b"/>
  <text x="600" y="437" font-family="system-ui,sans-serif" font-size="22" font-weight="600" fill="#0f0f10" text-anchor="middle">Enroll Now</text>
</svg>
EOF
```

> **Note:** For production, replace `frontend/public/og-image.png` with a proper 1200×630 PNG image. Use Figma, Canva, or a screenshot tool. The image should show the course name and branding clearly.

- [ ] **Step 10.2: Commit**

```bash
git add frontend/public/
git commit -m "feat(seo): add og-image placeholder (replace with final design)"
```

---

## Task 11: Verification

- [ ] **Step 11.1: Run full build**

```bash
cd frontend && npm run build 2>&1 | tail -15
```

Expected: Build completes with no TypeScript or plugin errors.

- [ ] **Step 11.2: Verify robots.txt is in dist**

```bash
ls dist/robots.txt dist/sitemap.xml
```

Expected: Both files present.

- [ ] **Step 11.3: Verify pre-rendered pages have real content**

```bash
grep -l "Build a Full Production" dist/index.html 2>/dev/null || grep "Build a Full" dist/index.html | head -2
```

Expected: Title text appears in the static HTML.

- [ ] **Step 11.4: Verify each page has unique title**

```bash
grep "<title>" dist/index.html
grep "<title>" dist/about/index.html 2>/dev/null
grep "<title>" dist/faq/index.html 2>/dev/null
```

Expected: Each file shows a different title.

- [ ] **Step 11.5: Verify noIndex pages have robots meta**

Open `dist/index.html` in a browser or inspect a page component. Auth pages must have `<meta name="robots" content="noindex,nofollow">` in their rendered output.

- [ ] **Step 11.6: Validate sitemap structure**

```bash
cat dist/sitemap.xml | grep "<loc>" | wc -l
```

Expected: 11 (one per public page).

- [ ] **Step 11.7: Update docs**

In `docs/features/LANDING_PAGE.md`, add under a new section `## SEO`:

```markdown
## SEO

All SEO is managed via `react-helmet-async`.

**Component:** `frontend/src/components/shared/SEO.tsx`  
**Config:** `frontend/src/lib/seo-config.ts`  
**Pre-rendering:** `vite-plugin-prerender` builds static HTML for 11 public routes  
**Structured data:** JSON-LD on Landing (Course schema), FAQ (FAQPage schema), About (Person schema)  
**Files:** `frontend/public/robots.txt`, `frontend/public/sitemap.xml`, `frontend/public/og-image.png`

### Update rule
When adding a new public page:
1. Add its SEO data to `SEO_PAGES` in `seo-config.ts`
2. Add `<SEO page={SEO_PAGES.yourPage} />` to the page component
3. Add its URL to `sitemap.xml`
4. Add its route to the prerender list in `vite.config.ts`
```

- [ ] **Step 11.8: Final commit**

```bash
cd ..
git add -A
git commit -m "docs: update LANDING_PAGE.md with SEO section and update rules"
git push origin main
```

---

## Post-Deploy Verification Checklist

After deploying to production, verify with these external tools:

- [ ] **Google Search Console** — Submit sitemap at `https://yousef-abdallah.online/sitemap.xml`
- [ ] **Rich Results Test** — Test `https://yousef-abdallah.online/` for Course schema
- [ ] **Rich Results Test** — Test `https://yousef-abdallah.online/faq` for FAQPage schema
- [ ] **Meta Tags Preview** — https://metatags.io — verify og:image shows correctly
- [ ] **Twitter Card Validator** — verify twitter:image renders
- [ ] **curl test** — `curl https://yousef-abdallah.online/ | grep "Build a Full"` — must return content
- [ ] **robots.txt** — visit `https://yousef-abdallah.online/robots.txt` in browser
- [ ] **sitemap.xml** — visit `https://yousef-abdallah.online/sitemap.xml` in browser
